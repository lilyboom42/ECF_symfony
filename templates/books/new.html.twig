{# templates/books/new.html.twig #}
{% extends 'base.html.twig' %}

{% block title %}Add New Book{% endblock %}

{% block body %}
    <h1>Add a New Book</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-primary">Save</button>
    {{ form_end(form) }}

<a href="{{ path('app_books_list') }}">Back to list</a> 
{% endblock %}
